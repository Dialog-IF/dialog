%% dgdebug -u cloak-tests.dg cloak.dg unit.dg stdlib.dg

(clean up $)
    (move player to #in #foyer)
	(now) (#cloak is #wornby #player)
	(now) ~(hook point awarded)
	(now) ~(message has been trampled)
	(now) ~(game has ended)
	(current score $Score)
	(decrease score by $Score)
	(clear all pronouns)
	(update environment around player)

#dont-go-north
%%(test *)
(run *)
	(current player $Player)
	($Player is #in #foyer)
	(collect words)
	    (perform [go #north])
	(into [you've only just arrived | $])
	($Player is #in #foyer)

#dark-if-wearing-cloak
(test *)
(run *)
	(move player to #in #bar)
	~(player can see)

#dont-drop-cloak
(test *)
(run *)
    (perform [drop #cloak])
    (#cloak is #wornby #player)

#hang-on-hook
(test *)
(set up *)
    (move player to #in #cloakroom)

(run *)
    (perform [put #cloak #on #hook])
	(#cloak is #on #hook)
    ~(#cloak is #wornby #player)

#score-for-hanging-cloak
(test *)
(set up *)
    (move player to #in #cloakroom)

(run *)
    (current score 0)
    (perform [put #cloak #on #hook])
	(current score 1)

(clean up *)

#warn-about-blundering
(test *)
(set up *)
	(move player to #in #bar)

(run *)
	~(player can see)
	(collect words) (perform [dance]) (into [in the dark | $])

#light-if-not-wearing-cloak
(test *)
(set up *)
    (move player to #in #cloakroom)
    (perform [put #cloak #on #hook])

(run *)
	(move player to #in #bar)
	(player can see)

#destroy-message
(test *)
(set up *)
    (move player to #in #bar)

(run *)
    (perform [dance])
	(perform [dance])
	(message has been trampled)

#score-for-seeing-message
(test *)
(set up *)
    (move player to #in #cloakroom)
    (perform [put #cloak #on #hook])
	(move player to #bar)

(run *)
	(player can see)
    (current score 1)
	~(message has been trampled)
    (collect words) (perform [examine #message]) (into [$ | won])
	(current score 2)

#score-for-examining-message
(test *)
(set up *)
    (move player to #in #cloakroom)
    (perform [put #cloak #on #hook])
	(move player to #bar)

(run *)
	(player can see)
    (current score 1)
	~(message has been trampled)
    (collect words) (perform [examine #message]) (into [$ | won])
	(current score 2)

#defeat-message
(test *)
(set up *)
	(move player to #in #bar)
	(perform [dance])
	(perform [dance])
    (move player to #in #cloakroom)
    (perform [put #cloak #on #hook])
	(move player to #bar)

(run *)
	(player can see)
	(current score 1)
	(message has been trampled)
    (collect words) (perform [examine #message]) (into [$ | lost])
	(current score 1)
