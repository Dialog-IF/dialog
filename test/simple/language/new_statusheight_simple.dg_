%% Test currently disabled because dfrotz doesn't support status bar height, and Z-machine is the only platform that cares

(style class @statusbar)
	height: 2em;

(do something) xyzzy

(program entry point)
	(status bar @statusbar with height 5) {
		(do something)
		(do something)
	}
